{
  "google_client_secret": "data/client_secret.json",
  "database": "data/studio.db",
  "ai_endpoint": "",
  "ai_api_key": "",
  "ai_model": "",
  "voice_endpoint": "",
  "voice_api_key": "",
  "voice_id": "",
  "voice_http_timeout": "90",
  "ffmpeg_path": "ffmpeg",
  "output_dir": "data/outputs",
  "locale": "vi-VN",
  "log_level": "INFO",
  "notes": [
    "Do not commit client_secret.json, OAuth tokens, real API keys, voice samples, or model weights.",
    "Voice Studio stores provider secrets in env/settings, not in project records.",
    "Local/offline voice generation uses Windows SAPI in the packaged .exe when available; HTTP/API dubbing requires your own provider endpoint.",
    "If your local/API provider needs external model weights, install/download them outside the repository."
  ]
}
